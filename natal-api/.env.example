# Optional. Required for POST /cron/daily-push.
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Then schedule: 5 * * * * curl -sS -X POST -H "Authorization: Bearer $CRON_SECRET" http://127.0.0.1:8000/cron/daily-push
CRON_SECRET=
